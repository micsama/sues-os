FROM python:3.9-alpine3.13

LABEL MAINTAINER="dextercai"

RUN mkdir /root/sues-os

WORKDIR /root/sues-os
COPY ./ ./

RUN pip3 install -r requirements-lite.txt --no-cache-dir \
    && rm -rf ~/.cache

CMD ["/bin/bash"]